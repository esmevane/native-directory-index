export const root = 'https://raw.githubusercontent.com'
export const repo = 'react-community/native-directory'
export const directory = `${root}/${repo}/master/react-native-libraries.json`
export const githubUrlFor = repo => `https://github.com/${repo}`
export const packageFor = repo =>
  `${root}/${repo}/master/package.json`
